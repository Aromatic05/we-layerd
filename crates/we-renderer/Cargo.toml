[package]
name = "we-renderer"
version = "0.2.7"
edition = "2021"

[dependencies]
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
we-renderer-sys = { path = "../we-renderer-sys" }
